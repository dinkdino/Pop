class_name: ApiTester
modules:
    enabled: [Laravel5, REST, ApiHelper, Asserts]
    config:
        Laravel5:
            environment_file: .env.testing

        PhpBrowser:
            url: http://pop.dev/
        REST:
            url: http://pop.dev/api/v1/